# testshm requires a very specific build directory layout, namely
# that `data/` is a subdirectory of the build directory.
# This just copies an empty file so the directory will exist.
fs = import('fs')

fs.copyfile('empty.txt')
