# texturec - bgfx texture compiler

texturec_sources = files(
  'texturec.cpp',
)

texturec = executable('texturec', texturec_sources,
  dependencies: [bx_dep, bimg_dep, bimg_decode_dep, bimg_encode_dep],
)
