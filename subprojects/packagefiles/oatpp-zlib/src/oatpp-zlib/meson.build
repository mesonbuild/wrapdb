header_files = files('EncoderProvider.hpp', 'Processor.hpp')

src_files += header_files

src_files += files('EncoderProvider.cpp', 'Processor.cpp')

install_headers(
  header_files,
  subdir: 'oatpp-zlib',
)
